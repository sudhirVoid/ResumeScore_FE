import { PriceCommaSeperatedPipe } from './price-comma-seperated.pipe';

describe('PriceCommaSeperatedPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceCommaSeperatedPipe();
    expect(pipe).toBeTruthy();
  });
});
